'use client'

import { Box, Container, Typography } from '@mui/material'

export default function AdminBookingsPage() {
  return (
    <Box sx={{ py: 8 }}>
      <Container maxWidth="lg">
        <Typography variant="h2">Admin Bookings (Coming Soon)</Typography>
      </Container>
    </Box>
  )
}
