'use client'

import { Box, Container, Typography } from '@mui/material'

export default function ContactsPage() {
  return (
    <Box sx={{ py: 8 }}>
      <Container maxWidth="lg">
        <Typography variant="h2">Contacts Page (Coming Soon)</Typography>
      </Container>
    </Box>
  )
}
