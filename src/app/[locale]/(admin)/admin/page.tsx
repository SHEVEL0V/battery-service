'use client'

import { Box, Container, Typography } from '@mui/material'

export default function AdminPage() {
  return (
    <Box sx={{ py: 8 }}>
      <Container maxWidth="lg">
        <Typography variant="h2">Admin Dashboard (Coming Soon)</Typography>
      </Container>
    </Box>
  )
}
